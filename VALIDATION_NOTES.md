# Validation notes

The release files were checked for the following before packaging:

- no `alpha_lower` selection logic or documentation remains;
- no local Dropbox, home-directory, or personal file-system paths remain;
- repository-relative input and output paths are used consistently;
- the main table generator includes the three detailed binary enrichment-path tables;
- the manuscript labels in `MANUSCRIPT_OUTPUTS.md` match the labels and filenames generated by the R table scripts;
- quick-check results and tables are isolated in `quick_test` subdirectories;
- brackets, braces, parentheses, comments, and quoted strings are balanced in all R files by static lexical checks.

R was not installed in the packaging environment, so the scripts could not be executed with `Rscript` here. Before public release, run the full analyses from a clean checkout and compare the generated values with the submitted manuscript.
